var should = require('should');

describe('Video', function() {
    'use strict';

    describe('get the type and the code from a video url', function() {

        it('is a youtube video', function(done) {
            should.notEqual(1, 2);
            done();
        });

    });
});
